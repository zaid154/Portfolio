# multer-storage-cloudinary@4 still pins peer cloudinary@^1, but the app uses
# cloudinary@2 (v2 API). The combo works fine at runtime; this lets npm install
# resolve past the stale peer range instead of erroring with ERESOLVE.
legacy-peer-deps=true
