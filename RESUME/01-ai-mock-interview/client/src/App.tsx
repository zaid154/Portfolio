// Placeholder entry component — scaffold only.
// Build the real MockMate AI UI inside src/pages and src/components.
export default function App() {
  return (
    <main style={{ display: 'grid', placeItems: 'center', minHeight: '100dvh' }}>
      <div style={{ textAlign: 'center', padding: '2rem' }}>
        <h1 style={{ margin: 0 }}>MockMate AI</h1>
        <p style={{ opacity: 0.7 }}>
          Scaffold ready — add features inside <code>src/</code>.
        </p>
      </div>
    </main>
  )
}
